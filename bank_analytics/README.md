# Система анализа банковских выписок

Универсальная система для анализа банковских выписок с автоматической категоризацией транзакций и формированием отчетности.

## Возможности

- 📊 **Автоматический парсинг** банковских выписок (CSV, Excel, TXT)
- 🏷️ **Умная категоризация** транзакций по ключевым словам
- 📈 **Детальная аналитика** доходов и расходов
- 📱 **Современный веб-интерфейс** с графиками и таблицами
- 📋 **Экспорт отчетов** в Excel и CSV
- 🔍 **Поиск и фильтрация** транзакций
- 📊 **Визуализация данных** с помощью графиков

## Установка

1. Клонируйте репозиторий или скачайте файлы
2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Запуск

### Веб-приложение (рекомендуется)
```bash
python app.py
```
Откройте браузер и перейдите по адресу: http://localhost:5000

### Консольная версия
```bash
python bank_analytics.py
```

## Использование

### Веб-интерфейс
1. Откройте http://localhost:5000
2. Загрузите файлы банковских выписок (перетащите или выберите файлы)
3. Просмотрите аналитику и отчеты
4. Экспортируйте данные в нужном формате

### Поддерживаемые форматы файлов
- **CSV** - с кодировками UTF-8, CP1251, Windows-1251
- **Excel** - .xlsx, .xls
- **TXT** - текстовые выписки с датами и суммами

### Автоматическая категоризация
Система автоматически определяет категории транзакций по ключевым словам:

- **Продукты**: магнит, пятерочка, перекресток, ашан, лента, продукты, еда, кафе, ресторан
- **Транспорт**: яндекс.такси, uber, метро, автобус, бензин, заправка, парковка, транспорт
- **Коммунальные**: жкх, электричество, газ, вода, отопление, коммунальные, квартплата
- **Связь**: мтс, билайн, мегафон, теле2, интернет, связь, мобильная связь
- **Здоровье**: аптека, больница, поликлиника, врач, лекарства, медицина
- **Образование**: школа, университет, курсы, обучение, образование
- **Развлечения**: кино, театр, концерт, игры, развлечения, отдых
- **Одежда**: одежда, обувь, магазин одежды, h&m, zara, uniqlo
- **Доходы**: зарплата, доход, перевод, возврат, проценты, дивиденды
- **Инвестиции**: акции, облигации, депозит, инвестиции, брокер

## Структура проекта

```
bank_analytics/
├── app.py                 # Веб-приложение Flask
├── bank_analytics.py      # Основная логика парсинга и аналитики
├── requirements.txt       # Зависимости Python
├── README.md             # Документация
├── templates/
│   └── index.html        # HTML шаблон
├── static/
│   ├── css/              # CSS стили
│   └── js/
│       └── app.js        # JavaScript логика
└── statements/           # Папка для загруженных файлов
```

## API Endpoints

- `POST /upload` - Загрузка файлов выписок
- `GET /summary` - Получение общей сводки
- `GET /categories` - Анализ по категориям
- `GET /monthly` - Месячный анализ
- `GET /trends` - Анализ трендов
- `GET /transactions` - Список транзакций с пагинацией
- `GET /export/excel` - Экспорт в Excel
- `GET /export/csv` - Экспорт в CSV
- `POST /update_category` - Обновление категории транзакции

## Примеры использования

### Загрузка файлов через API
```javascript
const formData = new FormData();
formData.append('files', fileInput.files[0]);

fetch('/upload', {
    method: 'POST',
    body: formData
})
.then(response => response.json())
.then(data => console.log(data));
```

### Получение аналитики
```javascript
fetch('/summary')
.then(response => response.json())
.then(data => {
    console.log('Доходы:', data.total_income);
    console.log('Расходы:', data.total_expense);
    console.log('Баланс:', data.balance);
});
```

## Настройка

### Добавление новых категорий
Отредактируйте словарь `category_rules` в классе `BankStatementParser`:

```python
self.category_rules = {
    "Новая категория": ["ключевое_слово1", "ключевое_слово2"],
    # ... остальные категории
}
```

### Изменение порта веб-приложения
В файле `app.py` измените параметр `port`:

```python
app.run(debug=True, host='0.0.0.0', port=8080)  # Новый порт
```

## Требования

- Python 3.7+
- Flask 2.3+
- pandas 2.1+
- numpy 1.24+
- openpyxl 3.1+

## Лицензия

MIT License

## Поддержка

При возникновении проблем:
1. Проверьте формат загружаемых файлов
2. Убедитесь, что файлы содержат колонки с датами, суммами и описаниями
3. Проверьте кодировку файлов (поддерживаются UTF-8, CP1251, Windows-1251)
